{
  "name": "enoughwork-app",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "version": "0.1.7",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "node scripts/build.mjs",
    "signer": "tauri signer generate -w keys/enoughwork.key",
    "release:patch": "command -v cargo-release >/dev/null 2>&1 || cargo install cargo-release && cd src-tauri && cargo release patch --execute",
    "release:minor": "command -v cargo-release >/dev/null 2>&1 || cargo install cargo-release && cd src-tauri && cargo release minor --execute",
    "release:major": "command -v cargo-release >/dev/null 2>&1 || cargo install cargo-release && cd src-tauri && cargo release major --execute"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^8.0.14"
  },
  "dependencies": {
    "@floating-ui/dom": "^1.7.6",
    "@tauri-apps/plugin-autostart": "~2.5.1",
    "@tauri-apps/plugin-store": "~2.4.3",
    "@tauri-apps/plugin-updater": "^2.10.1"
  }
}
